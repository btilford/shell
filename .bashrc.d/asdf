# shellcheck disable=SC1090
if [[ "$ASDF_HOME" == "" ]]; then
  ASDF_HOME="$HOME/.asdf"
fi

if [[ -d "$ASDF_HOME" ]]; then
  source "$ASDF_HOME/asdf.sh"
  source "$ASDF_HOME/completions/asdf.bash"
fi
